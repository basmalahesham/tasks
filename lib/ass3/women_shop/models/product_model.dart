import 'package:flutter/material.dart';

class ProductModel {
  String? image;
  String? title;
  int? price;
  Color? color;
  int? id;
  ProductModel({this.image, this.title, this.price, this.color, this.id});
}
